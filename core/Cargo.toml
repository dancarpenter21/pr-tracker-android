[package]
name = "prtracker-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "prtracker_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
jni = "0.21"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
approx = "0.5"
